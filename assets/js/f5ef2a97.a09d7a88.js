"use strict";(globalThis.webpackChunk_8x8_developer_docs=globalThis.webpackChunk_8x8_developer_docs||[]).push([[39910],{78577:e=>{e.exports=JSON.parse('{"id":"2026-07-06-ring-group-management-api-update-members","date":"2026-07-06","products":["Administration"],"product":"Administration","channel":"","changeType":"Changed","title":"Ring Group Management API update-members","excerpt":"The Ring Group Management API guide now documents a dedicated endpoint for targeted membership changes. Instead of reading a ring group, mutating its full member list, and writing the whole object back with PUT, you can send only the members that are changing.","hasArticle":true,"bodyHtml":"<p>The Ring Group Management API guide now documents a dedicated endpoint for <strong>targeted membership changes</strong>. Instead of reading a ring group, mutating its full member list, and writing the whole object back with <code>PUT</code>, you can send only the members that are changing.</p>\\n<h2>Apply a targeted, atomic delta</h2>\\n<pre><code class=\\"language-http\\">POST /ring-groups/{ringGroupId}/update-members\\nContent-Type: application/vnd.ringgroups.update-members.v1+json\\n</code></pre>\\n<p>The request body carries up to three optional lists \u2014 <code>add</code>, <code>update</code>, and <code>remove</code> \u2014 and at least one must be non-empty. Each list accepts up to 200 members, and you identify each member by either its <code>extensionId</code> or <code>extensionNumber</code>. The same identifier must not appear in more than one list. The whole delta is applied atomically: either every change succeeds or none do.</p>\\n<pre><code class=\\"language-json\\">{\\n  &quot;add&quot;: [\\n    { &quot;extensionNumber&quot;: &quot;1005&quot;, &quot;sequenceNumber&quot;: 4, &quot;voicemailAccessEnabled&quot;: true }\\n  ],\\n  &quot;update&quot;: [\\n    { &quot;extensionNumber&quot;: &quot;1002&quot;, &quot;sequenceNumber&quot;: 1 }\\n  ],\\n  &quot;remove&quot;: [\\n    { &quot;extensionNumber&quot;: &quot;1003&quot; }\\n  ]\\n}\\n</code></pre>\\n<p>Like the other write operations in the suite, the endpoint responds <code>202 Accepted</code> with an Operation resource (<code>operationType: UPDATE_MEMBERS</code>) that you poll to completion. This is a non-breaking addition \u2014 the existing endpoints, including full-object <code>PUT</code>, are unchanged, so if you genuinely need to replace the entire member list you can still do so.</p>\\n<p>The guide also explains how <code>sequenceNumber</code> is resolved for <code>ROUND_ROBIN</code> and <code>SEQUENTIAL</code> ring patterns: supplied values are treated as desired positions, must be unique within a single request, and may collide with an existing member&#39;s position (existing members shuffle down to make space). Omitting <code>sequenceNumber</code> on an <code>add</code> appends the new member to the end of the sequence.</p>\\n<ul>\\n<li><a href=\\"/administration/docs/ring-group-management-api-guide#use-case-4-manage-ring-group-members\\">Use Case 4: Manage Ring Group Members \u2014 Ring Group Management API Guide</a></li>\\n<li><a href=\\"/administration/changelog\\">Administration API Changelog</a></li>\\n</ul>\\n"}')}}]);