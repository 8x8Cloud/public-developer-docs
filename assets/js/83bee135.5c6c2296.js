"use strict";(globalThis.webpackChunk_8x8_developer_docs=globalThis.webpackChunk_8x8_developer_docs||[]).push([[84085],{26800:e=>{e.exports=JSON.parse('{"id":"2026-08-24-override-turn-relay-region","date":"2026-08-24","products":["JaaS"],"product":"JaaS","channel":"","changeType":"Added","title":"Override the TURN relay region","excerpt":"You can now pin a meeting\'s TURN relay to a specific region using the experimental iceServers option on the JitsiMeetExternalAPI constructor. This is separate from pinning the meeting to a regional data center: that controls where the media bridge (JVB) lives, while iceServers controls which TURN relay a participant\'s\u2026","hasArticle":true,"bodyHtml":"<p>You can now pin a meeting&#39;s TURN relay to a specific region using the experimental <code>iceServers</code> option on the <code>JitsiMeetExternalAPI</code> constructor. This is separate from pinning the meeting to a regional data center: that controls where the media bridge (JVB) lives, while <code>iceServers</code> controls which TURN relay a participant&#39;s media is routed through when they can&#39;t reach the bridge directly (for example, behind a restrictive firewall). By default, JaaS selects the relay it considers optimal for each participant.</p>\\n<h2>How it works</h2>\\n<p>The <code>iceServers</code> option takes a list of <code>replace</code> rules. Each rule matches existing ICE servers by <code>targetType</code> (<code>stun</code>, <code>turn</code>, or <code>turns</code>) and replaces their <code>urls</code>:</p>\\n<pre><code class=\\"language-javascript\\">const api = new JitsiMeetExternalAPI(&quot;8x8.vc&quot;, {\\n    roomName: &quot;vpaas-magic-cookie-YOUR_APP_ID/YOUR_ROOM&quot;,\\n    jwt: &quot;YOUR_JWT&quot;,\\n    parentNode: document.querySelector(&#39;#jaas-container&#39;),\\n\\n    iceServers: {\\n        replace: [\\n            {\\n                targetType: &#39;turn&#39;,\\n                urls: &#39;turn:prod-8x8-eu-central-1-turnrelay-oracle.jitsi.net:443?transport=udp&#39;\\n            },\\n            {\\n                targetType: &#39;turns&#39;,\\n                urls: &#39;turns:prod-8x8-eu-central-1-turnrelay-oracle.jitsi.net:443?transport=tcp&#39;\\n            }\\n        ]\\n    }\\n});\\n</code></pre>\\n<ul>\\n<li><code>targetType</code> \u2014 which ICE server entry to match: <code>stun</code>, <code>turn</code>, or <code>turns</code>.</li>\\n<li><code>urls</code> \u2014 the replacement URL(s), as a string or array of strings. Set to <code>null</code> to remove the entry.</li>\\n<li><code>username</code> / <code>credential</code> \u2014 leave unchanged so the relay keeps using the short-lived credentials JaaS issues for the meeting.</li>\\n</ul>\\n<p>The regional TURN relay hostnames are documented in the <a href=\\"/jaas/docs/faq\\">FAQ</a>, covering Tokyo, Mumbai, Sydney, Toronto, Frankfurt, London, Bahrain, S\xe3o Paulo, N. Virginia, and Oregon.</p>\\n<p><code>iceServers</code> is experimental, only takes effect via the iFrame API, and may change in a future release.</p>\\n"}')}}]);